# AI 브리핑룸 디자인 시스템

## 1. 제품 목적

AI 브리핑룸은 바쁜 직장인이 최신 AI 이슈를 빠르게 이해하고 실제 업무에 적용할 아이디어를 얻도록 돕는 한국어 기술 미디어다. 개발자와 IT 실무자는 같은 글에서 근거 자료와 기술적 세부사항까지 확인할 수 있어야 한다.

## 2. 브랜드 보이스

- 친근하지만 객관적인 리포터처럼 쓴다.
- 어려운 용어는 풀어 쓰되 정확성을 희생하지 않는다.
- 사실, 분석, 전망, 권고를 서로 다른 문장으로 구분한다.
- 과장된 최상급 표현과 검증되지 않은 단정을 피한다.
- 브랜드 표기: `AI 브리핑룸`
- 슬로건: `일하는 사람의 AI 브리핑`

## 3. 콘텐츠 계층

### 홈

1. 최근 발행 글 3개를 수동으로 탐색하는 대표 슬라이더
2. 추천 여부와 무관한 전체 최신 글
3. 전체 아티클 옆의 작은 WORK 업무 활용 추천 패널

슬라이더·WORK 글은 전체 최신 글에도 다시 노출한다. 앞의 영역은 최근·업무 활용 맥락을, 전체 최신 글은 시간순 탐색을 제공하므로 중복은 의도된 정보 구조다.

### 글 상세

1. 제목과 한 줄 요약
2. 예상 읽기 시간, 기준일, 작성 주체
3. 30초 요약
4. 무슨 일인가
5. 왜 중요한가
6. 업무에 어떻게 쓸까
7. 한계와 주의점
8. 참고자료와 관련 글

기존 글은 새 레이아웃 안에서 그대로 읽을 수 있어야 하며, 새 자동 생성 글부터 표준 섹션을 적용한다.

## 4. 색상 토큰

색상은 CSS 의미 토큰으로만 사용한다.

| 토큰 | 라이트 | 다크 | 역할 |
|---|---|---|---|
| background | warm paper | deep ink | 전체 배경 |
| surface | clean paper | raised ink | 주요 콘텐츠 표면 |
| foreground | near black | warm white | 본문 |
| muted | warm gray | slate | 보조 표면 |
| muted-foreground | medium gray | light gray | 메타 정보 |
| accent | report blue | clear blue | 링크·핵심 행동 |
| signal | calm teal | soft teal | 업무 적용 표시 |
| border | warm line | dark line | 구분선 |

## 5. 타이포그래피

- 본문과 UI: Pretendard Variable, Pretendard, Noto Sans KR, system-ui
- 코드: Google Sans Code, ui-monospace
- 본문 기본 행간: 1.75 이상
- 글 본문 최대 너비: 약 46rem
- 제목은 화면 크기에 따라 유연하게 줄이되 모바일에서 과도하게 커지지 않는다.
- 영문 대문자와 넓은 자간은 짧은 분류 레이블에만 사용한다.

## 6. 레이아웃과 컴포넌트

- 전체 콘텐츠 폭은 약 72rem, 본문은 약 46rem로 제한한다.
- 홈 첫 화면은 장식용 히어로 대신 최신 핵심 글과 가치 제안을 보여준다.
- 최근 글 슬라이더는 최신 3개 글만 노출하고 자동 재생하지 않는다. 이전·다음 버튼은 순환 탐색하며 현재 위치를 알린다.
- 데스크톱 홈의 전체 아티클은 넓은 주열, WORK는 좁은 보조 열로 구성한다. 모바일에서는 전체 아티클 다음에 WORK를 배치한다.
- 홈은 Toss Tech의 편집형 위계에서 구조만 참고하고, AI 브리핑룸의 색상·서체·문구를 유지한다.
- 게시물 카드는 제목, 설명, 날짜, 읽기 시간, 업무 적용 신호 순으로 읽힌다.
- 업무 활용 추천은 최신 순번이 아니라 제목·태그의 명시적 업무 신호로 선정하며, 최신성을 동점 기준으로 사용한다.
- 둥근 모서리는 작은 반경으로 제한하고 카드 중첩을 만들지 않는다.
- 데스크톱 글 상세에서만 목차를 보조 열에 고정한다.
- 링크와 버튼은 모바일에서 최소 44×44 CSS px 이상의 조작 영역과 명확한 포커스를 제공한다.
- Mermaid 도형은 라이트·다크 테마 토큰으로 렌더링하고, 커스텀 도형색에는 실제 채움색 기준의 고대비 글자색을 적용한다.

## 7. 모션과 상태

- 색상과 위치 전환은 120~200ms 범위로 제한한다.
- 최근 글 슬라이더는 이전·다음 방향을 반영한 `transform + opacity` 전환을 180ms로 사용한다.
- 모바일의 링크와 버튼은 터치하는 동안 투명도 또는 크기가 즉시 변해 입력 수신 여부를 보여준다.
- 콘텐츠를 가리는 진입 애니메이션을 사용하지 않는다.
- `prefers-reduced-motion`에서는 스크롤·슬라이드 전환을 제거하되 상태 변경 결과는 즉시 표시한다.
- hover만으로 의미를 전달하지 않는다.

## 8. 금지 패턴

- 글래스모피즘, 큰 블러 광원, 장식용 그라디언트
- 의미 없는 대형 영문 히어로 문구
- 카드 안의 카드와 과도한 알약형 배지
- 출처 없는 방문자 카운터와 잘못된 소셜 링크
- 영어와 한국어가 뒤섞인 내비게이션
- 제목 전체가 링크의 접근 가능한 이름이 되어 지나치게 길어지는 불필요한 부가 레이블

## 9. Agent Prompt Guide

AI 브리핑룸 UI를 수정할 때는 다음을 지킨다.

1. 기존 Astro 구조와 콘텐츠 호환성을 유지한다.
2. `theme.css`의 의미 토큰을 사용하고 컴포넌트에 색상값을 직접 넣지 않는다.
3. 한국어 UI 문구와 Asia/Seoul 기준을 사용한다.
4. 모바일 320px부터 설계하고 768px, 1024px, 1440px에서 확인한다.
5. 의미 있는 HTML, 보이는 키보드 포커스, 충분한 대비를 제공한다.
6. 새 글은 `30초 요약`, `업무에 어떻게 쓸까`, `한계와 주의점`, `참고자료` 구조를 우선한다.
7. 장식보다 정보 탐색 속도와 읽기 편의성을 우선한다.
